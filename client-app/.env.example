# OAuth Client Configuration Example
# Copy this file to .env and update with your actual values

# OAuth Client ID from your authorization server
CLIENT_ID=your_client_id_here

# OAuth Client Secret from your authorization server
CLIENT_SECRET=your_client_secret_here

# Authorization Server URL
AUTH_SERVER=http://localhost:3000

# Redirect URI after authentication (must match the registered redirect URI)
REDIRECT_URI=http://localhost:3001/callback
